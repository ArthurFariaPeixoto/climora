/**
 * Previsão por dia.
 *
 * Exibe o `DailyForecast`, agregado dos blocos de 3h por `utils/selectors`.
 *
 * TODO: implementar a lista diária quando a feature de clima for desenvolvida.
 */
export function DailyForecast() {
  return null;
}
