/**
 * Bloco de gráficos do dashboard.
 *
 * Único componente que utiliza Recharts. Deve ser carregado de forma preguiçosa
 * (`React.lazy` + `Suspense`) para manter o chunk separado do load inicial
 * (arquitetura §13).
 *
 * TODO: implementar os gráficos (temperatura por hora/dia, precipitação) com
 * Recharts quando a feature de clima for desenvolvida.
 */
export function WeatherCharts() {
  return null;
}
