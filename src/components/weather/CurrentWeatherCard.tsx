/**
 * Card de condição climática atual.
 *
 * Recebe o modelo `CurrentWeather` já formatado do `use-weather` e exibe
 * temperatura, sensação térmica e condição.
 *
 * TODO: implementar o card quando a feature de clima for desenvolvida.
 */
export function CurrentWeatherCard() {
  return null;
}
