/**
 * Grade de métricas (umidade, vento, máxima, mínima etc.).
 *
 * Compõe múltiplos `MetricTile` recebendo dados já formatados do
 * `use-weather`.
 *
 * TODO: implementar a grade quando a feature de clima for desenvolvida.
 */
export function MetricsGrid() {
  return null;
}
