/**
 * Barra de busca de cidades.
 *
 * Guarda o termo digitado (estado local — §7) e colapsa o hook de feature
 * `use-city-search`. Única ponte entre a UI de busca e a lógica.
 *
 * TODO: implementar campo, validação, submissão e acessibilidade
 * (combobox/listbox com ARIA) quando a feature for desenvolvida.
 */
export function SearchBar() {
  return null;
}
