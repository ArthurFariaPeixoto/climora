/**
 * Validações de busca (funções puras).
 *
 * TODO: implementar as regras (termo vazio, muito curto, caracteres inválidos)
 * quando a feature de busca for desenvolvida.
 */
export function validateSearchTerm(_term: string): string | null {
  throw new Error('Not implemented');
}
