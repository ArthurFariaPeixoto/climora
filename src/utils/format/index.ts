/**
 * Formatters de exibição (funções puras, testáveis sem mock).
 *
 * TODO: implementar as regras de formatação (temperatura, vento, data/hora,
 * umidade) quando o dashboard for desenvolvido.
 */
export function formatTemperature(_tempC: number): string {
  throw new Error('Not implemented');
}
