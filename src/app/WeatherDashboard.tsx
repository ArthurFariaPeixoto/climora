/**
 * Composição raiz do dashboard.
 *
 * Responsável por manter a cidade selecionada (estado elevado), consumir os
 * hooks de feature (`use-weather`, `use-city-search`) e distribuir dados e
 * estados para os widgets. Não contém lógica de negócio nem conhece a API.
 *
 * TODO: implementar a composição dos widgets (search + weather + state)
 * quando as features forem desenvolvidas.
 */
export function WeatherDashboard() {
  return null;
}
