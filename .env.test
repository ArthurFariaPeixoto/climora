# Ambiente de teste (Vitest — modo `test`). Valores fake, apenas para os
# testes de integração de `services/http` e `services/repositories` via MSW.
# Nunca use chaves reais aqui; o arquivo é commitado por não ser secreto.
VITE_WEATHER_API_KEY=tests-only-key
VITE_WEATHER_API_BASE_URL=http://localhost:3003